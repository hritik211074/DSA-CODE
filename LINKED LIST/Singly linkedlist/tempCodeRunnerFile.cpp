int i=1;
    // Node *pre=head;
    // while(i<position-1){
    //     pre=pre->next;
    //     i++;
    // }
    // Node *current=pre->next;

    // pre->next=current->next;
    // current->next=NULL;
    // delete current; 
